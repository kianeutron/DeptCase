/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost'],
        remotePatterns: [],
        unoptimized: true,
    },
}

module.exports = nextConfig 